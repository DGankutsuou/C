#include<stdio.h>
int main()
{
    
    int a = 5, b = 5, c = 10, result;
    result = (a > b);
    printf("(a > b) is %d", result);
    // res = a % b;
    // printf("%d\n", a--);
    // printf("%d", a);
    // int a = 6, c = 6;
    // printf("%d != %d (%d)", a, c, a != c);

    return 0;
}